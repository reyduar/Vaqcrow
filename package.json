{
  "name": "vaqcrow",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.27.0",
  "engines": {
    "node": ">=24.0.0 <25.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:db": "supabase test db --local supabase/tests",
    "test:e2e": "turbo run test:e2e",
    "test:e2e:install": "pnpm --filter @vaqcrow/web run test:e2e:install",
    "boundaries": "depcruise apps/*/src packages/*/src --config .dependency-cruiser.cjs",
    "test:boundaries": "vitest run",
    "verify": "pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run build && pnpm run boundaries && pnpm run test:boundaries",
    "install:verify": "pnpm install --frozen-lockfile",
    "workspaces:list": "pnpm -r list --depth -1",
    "env:docker:up": "./scripts/local-env.sh up",
    "env:docker:down": "./scripts/local-env.sh down",
    "env:docker:status": "./scripts/local-env.sh status",
    "dev:web:docker": "pnpm --filter @vaqcrow/web run dev:docker",
    "dev:web:cloud": "pnpm --filter @vaqcrow/web run dev:cloud"
  },
  "devDependencies": {
    "turbo": "^2.10.13",
    "dependency-cruiser": "^16.0.0",
    "typescript": "catalog:",
    "eslint": "catalog:",
    "@eslint/js": "catalog:",
    "typescript-eslint": "catalog:",
    "globals": "catalog:",
    "vitest": "catalog:",
    "eslint-config-next": "^16.3.5",
    "@vaqcrow/contracts": "workspace:*",
    "@vaqcrow/domain": "workspace:*",
    "@vaqcrow/ai": "workspace:*"
  }
}
